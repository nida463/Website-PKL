import React from 'react';
import '../../styles/Navbar.css'; // CSS untuk Logo

const Logo = () => {
  return (
    <div className="logo">
      <h1>Logo</h1> {/* Menggunakan elemen h1 untuk menampilkan teks logo */}
    </div>
  );
};

export default Logo;
