// Logo.js
import React from 'react';
import LogoGroup from './LogoGroup';
import './Logo.css'; // CSS for the Logo organism

const Logo = () => {
    return (
        <div className="logo-bar">
            <LogoGroup />
        </div>
    );
};

export default Logo;
